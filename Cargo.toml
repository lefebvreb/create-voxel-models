[package]
name = "voxels"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
name = "voxels"
crate-type = ["cdylib"]

[dependencies]
either = { version = "1.17.0", default-features = false }
pyo3 = { version = "0.29.1", features = ["either"] }
