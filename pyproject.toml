[build-system]
requires = ["maturin>=1.14,<2.0"]
build-backend = "maturin"

[project]
name = "voxels"
requires-python = ">=3.8"
dynamic = ["authors", "description", "keywords", "license", "version"]

[tool.maturin]
python-source = "python"
module-name = "voxels._voxels"
